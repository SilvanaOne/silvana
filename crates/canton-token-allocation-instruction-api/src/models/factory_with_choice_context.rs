/*
 * allocation instruction off-ledger API
 *
 * Implemented by token registries for using and managing allocation instructions by wallets. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// FactoryWithChoiceContext : A factory contract together with the choice context required to exercise the choice provided by the factory. Typically used to implement the generic initiation of on-ledger workflows via a Daml interface.  Clients SHOULD avoid reusing the same `FactoryWithChoiceContext` for exercising multiple choices, as the choice context MAY be specific to the choice being exercised. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct FactoryWithChoiceContext {
    /// The contract ID of the contract implementing the factory interface.
    #[serde(rename = "factoryId")]
    pub factory_id: String,
    #[serde(rename = "choiceContext")]
    pub choice_context: Box<models::ChoiceContext>,
}

impl FactoryWithChoiceContext {
    /// A factory contract together with the choice context required to exercise the choice provided by the factory. Typically used to implement the generic initiation of on-ledger workflows via a Daml interface.  Clients SHOULD avoid reusing the same `FactoryWithChoiceContext` for exercising multiple choices, as the choice context MAY be specific to the choice being exercised. 
    pub fn new(factory_id: String, choice_context: models::ChoiceContext) -> FactoryWithChoiceContext {
        FactoryWithChoiceContext {
            factory_id,
            choice_context: Box::new(choice_context),
        }
    }
}

