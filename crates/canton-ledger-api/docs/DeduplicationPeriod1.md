# DeduplicationPeriod1

## Enum Variants

| Name | Description |
|---- | -----|
| DeduplicationPeriod1OneOf | The actual deduplication window used for the submission, which is derived from &#x60;&#x60;Commands.deduplication_period&#x60;&#x60;. The ledger may convert the deduplication period into other descriptions and extend the period in implementation-specified ways.  Used to audit the deduplication guarantee described in &#x60;&#x60;commands.proto&#x60;&#x60;.  Optional; the deduplication guarantee applies even if the completion omits this field. |
| DeduplicationPeriod1OneOf1 | The actual deduplication window used for the submission, which is derived from &#x60;&#x60;Commands.deduplication_period&#x60;&#x60;. The ledger may convert the deduplication period into other descriptions and extend the period in implementation-specified ways.  Used to audit the deduplication guarantee described in &#x60;&#x60;commands.proto&#x60;&#x60;.  Optional; the deduplication guarantee applies even if the completion omits this field. |
| DeduplicationPeriod1OneOf2 | The actual deduplication window used for the submission, which is derived from &#x60;&#x60;Commands.deduplication_period&#x60;&#x60;. The ledger may convert the deduplication period into other descriptions and extend the period in implementation-specified ways.  Used to audit the deduplication guarantee described in &#x60;&#x60;commands.proto&#x60;&#x60;.  Optional; the deduplication guarantee applies even if the completion omits this field. |

[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


